-- Database schema placeholder
-- MongoDB is schemaless but we define expected indexes

-- medications:
--   index on (is_active)
--   index on (name)

-- medication_schedules:
--   index on (medication_id)

-- doses:
--   index on (medication_id)
--   index on (dose_time)
--   index on (taken)
